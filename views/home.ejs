<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Hi Hello, This is the home page</h1>
    <button id="myButton">Click me</button>
  </body>
  <script>
    const accessToken = localStorage.getItem("accessToken"); // Retrieve token from local storage
    const button = document.getElementById("myButton");
    const clientId = "2rvj8vni1s7nvgbk7vk18mktj";
    const domainName = "nags";
    const redirectUri = "http%3A%2F%2Flocalhost%3A8000";
    const login_url = `https://${domainName}.auth.ap-south-1.amazoncognito.com/login?client_id=${clientId}&response_type=token&scope=email+openid+phone&redirect_uri=${redirectUri}`;

    function login() {
      fetch("/login", {
        method: "GET",
      })
        .then((response) => {
          console.log(response);
        })
        .catch((error) => {
          console.log(error);
        });
    }
    button.addEventListener("click", login);
  </script>
</html>
